install_data(
  '_'.join([app_id + '.svg']),
  install_dir: join_paths(app_datadir, 'icons', 'hicolor', 'scalable', 'apps'),
)

install_data(
  '_'.join([app_id + '-symbolic.svg']),
  install_dir: join_paths(app_datadir, 'icons', 'hicolor', 'symbolic', 'apps'),
)

